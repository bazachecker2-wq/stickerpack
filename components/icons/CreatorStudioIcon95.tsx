import React from 'react';

const CreatorStudioIcon95: React.FC<{ className?: string }> = ({ className }) => (
  <img src="https://i.imgur.com/MZaL7gV.png" alt="Creator Studio" className={className} style={{ imageRendering: 'pixelated' }} />
);

export default CreatorStudioIcon95;
