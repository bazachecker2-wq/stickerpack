import React from 'react';

const LogoutIcon95: React.FC<{ className?: string }> = ({ className }) => (
  <img src="https://i.imgur.com/gK2Z01p.png" alt="Logout" className={className} style={{ imageRendering: 'pixelated' }} />
);

export default LogoutIcon95;
